#include "main.h"

/**
 * _isalpha - checks for alphabetic character
 *
 * Return: success always
 *
 */

int _isalpha(int c)
{
	int x;
	if (x >= 65 && x <= 89; || x >= 97 && x <= 122)
		_putchar



	return (0);
}
