#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer
 *
 * @n: function argument
 *
 * Return: success always
 *
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
